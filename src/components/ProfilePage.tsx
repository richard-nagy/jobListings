import { FC, ReactElement } from 'react';

const ProfilePage: FC = (): ReactElement => {
    //#region Render
    return <div style={{
        padding: 40,
    }}>
        
    </div>;
    //#endregion
};

export default ProfilePage;
