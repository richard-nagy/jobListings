import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            main: "#00897B",
        },
        secondary: {
            main: "#FFB300",
        },
        background: {
            default: "#F5F5F5",
            paper: "#E0E0E0",
        },
        text: {
            primary: "#212121",
            secondary: "#757575",
        },
    },

});

export default theme;
